services:
  portfolio:
    build: .
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "3000:80"
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
    name: proxy_proxy-netzwerk
